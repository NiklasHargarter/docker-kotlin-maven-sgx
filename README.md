# docker-kotlin-maven-sgx

build the docker image with `docker build -t kotlin-sgx .`


<!-- BUILD THE IMAGE -->
## Build the image
If you wish, you can also build the image yourself.

```console
docker build -t kotlin-sgx:latest .
```